@model ProdBuilder0920.Domain.PackageSeries

@{
    ViewBag.Title = "UpdatePackageSeries";
}
<br/>
<br/>
<br/>
<h2>Update PackageSeries</h2>

@using (Html.BeginForm("UpdatePackageSeriesIn", "Admin", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>PackageSeries</h4>
        <hr />
        @Html.ValidationSummary(false, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.PackageSeriesId)

        <div class="form-group">
            @Html.LabelFor(model => model.PackageSeriesName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-6">
                @Html.EditorFor(model => model.PackageSeriesName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.PackageSeriesName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-success delete" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
